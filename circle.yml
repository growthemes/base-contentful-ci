machine:
  python:
    version: 2.7.11

test:
  override: ~
  
dependencies:
  cache_directories:
  - build
  - lib

deployment:
  prod:
    branch: master
    commands:
    - make deploy
  staging:
    branch: /^((?!master).)*$/
    commands:
    - make deploy
